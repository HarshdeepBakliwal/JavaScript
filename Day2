<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <script>
      // for loop
      for (i = 0; i <= 3; i++) {
        console.log("Hello World, I’m a good programmer.");
      }
      //function--> A set of code will be executed whenever we call it.
      function example() {
        console.log("Calling an function.");
      }
      example();

      // function with parameters
      function calc(num1, num2) {
        console.log("The sum of num1 and num2 is: " + (num1 + num2));
      }
      calc(9567 , 3658);
      // conditional statements
      function revise() {
        var first = parseInt(prompt("Enter the first number: "));
        var second = parseInt(prompt("Enter the second number: "));
        if (first > second) {
          console.log("You are a pro programmer.");
        } else {
          console.log(
            "You are not a good programmer but can be better with time."
          );
        }
      }
      revise();
      // using prompt keyword we can get input from user.

      function calculate() {
        var a = prompt("Got my first number as: ");
        var b = prompt("Got my second number as:");
        console.log(a * b);
      }
      calculate();

      function bar() {
        c = prompt("your fav marvel charcter: ");
        console.log("My favourite marvel character is: " + c);
      }
      bar();
    </script>
  </body>
</html>
